{
  "name": "mindcomb-t3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "npm install && vite build",
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview"
  }
}
